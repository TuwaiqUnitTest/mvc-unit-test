@{
    var word = (string)ViewData["word"];
    var colors = (List<(int, int, int)>)ViewData["colors"];
    @*var inx = "rgb(100,299,10)";*@
}
<h4> Your Query result: </h4>
<div>
    @for(var i=0; i< word.Length;i++)
    @*@foreach (var charr in word)*@
    {
        Console.WriteLine(colors[i] +" is for the char: "+ word[i]);
        var inxColor = "rgb"+@colors[i];
        Console.WriteLine(inxColor);

    <span style="background-color:@inxColor; font-size:100px;"> @word[i] </span>
    }
</div>
